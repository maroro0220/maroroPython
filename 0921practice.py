class Person:
      def __init__(self,name,phone=None):
            self.name=name
            self.phone=phone
      def __repr__(self):
            return 'name={} tel={}'.format(self.name,self.phone)
class Job:
      def __init(self.position,salary):
            self.position=position
            self.salary=salary
      def __repr__(self):
            return 'position={} salar{}'.format(self.position, self.salary)
